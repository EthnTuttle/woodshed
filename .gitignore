# Full-size vendor images and plan PDFs pulled during research: never commit, never publish.
assets/img/
assets/pdf/

# assets/thumb/ IS tracked on purpose — it is the ~4 MB that actually gets published, and CI
# needs it because assets/img/ above is not in the repo.

# Build output; regenerated by tools/build_pages.py
dist/

__pycache__/
*.pyc
.DS_Store
